# ponytail: pnpm 11's pre-run install check kept re-adding an invalid `allowBuilds`
# placeholder to pnpm-workspace.yaml and failing script runs. Builds are already
# approved via onlyBuiltDependencies, so skip the pre-run verify.
verify-deps-before-run=false
